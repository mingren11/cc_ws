<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>tros</name>
  <version>2.4.3</version>
  <description>TogetheROS Bot</description>
  <maintainer email="zhuo.wang@d-robotics.cc">zhuo</maintainer>
  <license>Apache License 2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>hobot-models-basic</depend>
  <depend>base</depend>

  <depend condition="$PLATFORM == X3">ai_msgs</depend>
  <depend condition="$PLATFORM == X3">audio_control</depend>
  <depend condition="$PLATFORM == X3">audio_msg</depend>
  <depend condition="$PLATFORM == X3">audio_tracking</depend>
  <depend condition="$PLATFORM == X3">body_tracking</depend>
  <depend condition="$PLATFORM == X3">dnn_benchmark_example</depend>
  <depend condition="$PLATFORM == X3">dnn_node</depend>
  <depend condition="$PLATFORM == X3">dnn_node_example</depend>
  <depend condition="$PLATFORM == X3">dnn_node_sample</depend>
  <depend condition="$PLATFORM == X3">elevation_net</depend>
  <depend condition="$PLATFORM == X3">gesture_control</depend>
  <depend condition="$PLATFORM == X3">hand_gesture_detection</depend>
  <depend condition="$PLATFORM == X3">hand_lmk_detection</depend>
  <depend condition="$PLATFORM == X3">hbm_img_msgs</depend>
  <depend condition="$PLATFORM == X3">hobot_audio</depend>
  <depend condition="$PLATFORM == X3">hobot_chatbot</depend>
  <depend condition="$PLATFORM == X3">hobot_codec</depend>
  <depend condition="$PLATFORM == X3">hobot_cv</depend>
  <depend condition="$PLATFORM == X3">hobot_falldown_detection</depend>
  <depend condition="$PLATFORM == X3">hobot_hdmi</depend>
  <depend condition="$PLATFORM == X3">hobot_image_publisher</depend>
  <depend condition="$PLATFORM == X3">hobot_llm</depend>
  <depend condition="$PLATFORM == X3">hobot_mot</depend>
  <depend condition="$PLATFORM == X3">hobot_shm</depend>
  <depend condition="$PLATFORM == X3">hobot_tts</depend>
  <depend condition="$PLATFORM == X3">hobot_usb_cam</depend>
  <depend condition="$PLATFORM == X3">hobot_vio</depend>
  <depend condition="$PLATFORM == X3">hobot_visualization</depend>
  <depend condition="$PLATFORM == X3">img_msgs</depend>
  <depend condition="$PLATFORM == X3">imu_sensor</depend>
  <depend condition="$PLATFORM == X3">line_follower_model</depend>
  <depend condition="$PLATFORM == X3">line_follower_perception</depend>
  <depend condition="$PLATFORM == X3">mipi_cam</depend>
  <depend condition="$PLATFORM == X3">mono2d_body_detection</depend>
  <depend condition="$PLATFORM == X3">mono2d_trash_detection</depend>
  <depend condition="$PLATFORM == X3">mono3d_indoor_detection</depend>
  <depend condition="$PLATFORM == X3">parking_perception</depend>
  <depend condition="$PLATFORM == X3">parking_search</depend>
  <depend condition="$PLATFORM == X3">rgbd_sensor</depend>
  <depend condition="$PLATFORM == X3">websocket</depend>
  <depend condition="$PLATFORM == X3">hobot_rtsp_client</depend>
  <depend condition="$PLATFORM == X3">tros_perception_fusion</depend>
  <depend condition="$PLATFORM == X3">tros_lowpass_filter</depend>
  <depend condition="$PLATFORM == X3">face_age_detection</depend>
  <depend condition="$PLATFORM == X3">face_landmarks_detection</depend>

  <depend condition="$PLATFORM == X5">ai_msgs</depend>
  <depend condition="$PLATFORM == X5">audio_msg</depend>
  <depend condition="$PLATFORM == X5">audio_control</depend>
  <depend condition="$PLATFORM == X5">audio_tracking</depend>
  <depend condition="$PLATFORM == X5">body_tracking</depend>
  <depend condition="$PLATFORM == X5">dnn_benchmark_example</depend>
  <depend condition="$PLATFORM == X5">dnn_node</depend>
  <depend condition="$PLATFORM == X5">dnn_node_example</depend>
  <depend condition="$PLATFORM == X5">dnn_node_sample</depend>
  <depend condition="$PLATFORM == X5">elevation_net</depend>
  <depend condition="$PLATFORM == X5">gesture_control</depend>
  <depend condition="$PLATFORM == X5">hand_gesture_detection</depend>
  <depend condition="$PLATFORM == X5">hand_lmk_detection</depend>
  <depend condition="$PLATFORM == X5">hbm_img_msgs</depend>
  <depend condition="$PLATFORM == X5">hobot_audio</depend>
  <depend condition="$PLATFORM == X5">hobot_codec</depend>
  <depend condition="$PLATFORM == X5">hobot_cv</depend>
  <depend condition="$PLATFORM == X5">hobot_falldown_detection</depend>
  <depend condition="$PLATFORM == X5">hobot_image_publisher</depend>
  <depend condition="$PLATFORM == X5">hobot_hdmi</depend>
  <depend condition="$PLATFORM == X5">hobot_mot</depend>
  <depend condition="$PLATFORM == X5">hobot_shm</depend>
  <depend condition="$PLATFORM == X5">hobot_usb_cam</depend>
  <depend condition="$PLATFORM == X5">hobot_vio</depend>
  <depend condition="$PLATFORM == X5">hobot_visualization</depend>
  <depend condition="$PLATFORM == X5">img_msgs</depend>
  <depend condition="$PLATFORM == X5">imu_sensor</depend>
  <depend condition="$PLATFORM == X5">mipi_cam</depend>
  <depend condition="$PLATFORM == X5">mono2d_body_detection</depend>
  <depend condition="$PLATFORM == X5">mono2d_trash_detection</depend>
  <depend condition="$PLATFORM == X5">mono3d_indoor_detection</depend>
  <depend condition="$PLATFORM == X5">parking_perception</depend>
  <depend condition="$PLATFORM == X5">parking_search</depend>
  <depend condition="$PLATFORM == X5">websocket</depend>
  <depend condition="$PLATFORM == X5">hobot_yolo_world</depend>
  <depend condition="$PLATFORM == X5">hobot_dosod</depend>
  <depend condition="$PLATFORM == X5">mono_mobilesam</depend>
  <depend condition="$PLATFORM == X5">mono_pwcnet</depend>
  <depend condition="$PLATFORM == X5">clip_msgs</depend>
  <depend condition="$PLATFORM == X5">clip_encode_image</depend>
  <depend condition="$PLATFORM == X5">clip_encode_text</depend>
  <depend condition="$PLATFORM == X5">clip_manage</depend>
  <depend condition="$PLATFORM == X5">hobot_stereonet</depend>
  <depend condition="$PLATFORM == X5">hobot_llamacpp</depend>
  <depend condition="$PLATFORM == X5">hobot_rtsp_client</depend>
  <depend condition="$PLATFORM == X5">tros_perception_fusion</depend>
  <depend condition="$PLATFORM == X5">tros_lowpass_filter</depend>
  <depend condition="$PLATFORM == X5">face_age_detection</depend>
  <depend condition="$PLATFORM == X5">face_landmarks_detection</depend>
  <depend condition="$PLATFORM == X5">hobot_stereonet_utils</depend>
  <depend condition="$PLATFORM == X5">hobot_zed_cam</depend>
  <depend condition="$PLATFORM == X5">dstereo_occnet</depend>
  <depend condition="$PLATFORM == X5">mono_edgesam</depend>
  <depend condition="$PLATFORM == X5">reid</depend>

  <depend condition="$PLATFORM == S100">ai_msgs</depend>
  <depend condition="$PLATFORM == S100">audio_msg</depend>
  <depend condition="$PLATFORM == S100">dnn_node</depend>
  <depend condition="$PLATFORM == S100">dnn_node_example</depend>
  <depend condition="$PLATFORM == S100">dstereo_occnet</depend>
  <depend condition="$PLATFORM == S100">hbm_img_msgs</depend>
  <depend condition="$PLATFORM == S100">hobot_codec</depend>
  <depend condition="$PLATFORM == S100">hobot_cv</depend>
  <depend condition="$PLATFORM == S100">hobot_image_publisher</depend>
  <depend condition="$PLATFORM == S100">hobot_shm</depend>
  <depend condition="$PLATFORM == S100">hobot_usb_cam</depend>
  <depend condition="$PLATFORM == S100">hobot_visualization</depend>
  <depend condition="$PLATFORM == S100">img_msgs</depend>
  <depend condition="$PLATFORM == S100">imu_sensor</depend>
  <depend condition="$PLATFORM == S100">mipi_cam</depend>
  <depend condition="$PLATFORM == S100">mono2d_body_detection</depend>
  <depend condition="$PLATFORM == S100">mono_edgesam</depend>
  <depend condition="$PLATFORM == S100">websocket</depend>
  <depend condition="$PLATFORM == S100">clip_msgs</depend>
  <depend condition="$PLATFORM == S100">clip_encode_image</depend>
  <depend condition="$PLATFORM == S100">clip_encode_text</depend>
  <depend condition="$PLATFORM == S100">clip_manage</depend>
  <depend condition="$PLATFORM == S100">hobot_dosod</depend>
  <depend condition="$PLATFORM == S100">hobot_stereonet</depend>
  <depend condition="$PLATFORM == S100">hobot_rtsp_client</depend>
  <depend condition="$PLATFORM == S100">hobot_stereonet_utils</depend>
  <depend condition="$PLATFORM == S100">hobot_bev</depend>
  <depend condition="$PLATFORM == S100">hobot_centerpoint</depend>
  <depend condition="$PLATFORM == S100">hobot_llamacpp</depend>
  <depend condition="$PLATFORM == S100">hobot_zed_cam</depend>
  <depend condition="$PLATFORM == S100">hobot_tts</depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
